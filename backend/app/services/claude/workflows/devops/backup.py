from .._types import WorkflowDef, register

register(WorkflowDef(
    id="backup_system",
    task_type="devops",
    label="Система резервного копирования",
    keywords=["бэкап", "pg_dump", "резервная копия", "backup", "восстановление данных"],
    credits_min=600,
    credits_max=1200,
    key_pause="после первого успешного бэкапа — проверь восстановление",
    questionnaire=(
        "1. Что бэкапить (PostgreSQL, файлы, Redis, всё)?\n"
        "2. Куда складывать (S3/YOS, локальный диск, удалённый сервер)?\n"
        "3. Расписание и глубина хранения (7 дней, 30 дней)?\n"
        "4. Шифрование бэкапов?\n"
        "5. Уведомления об успехе/ошибке?"
    ),
    phases=(
        "1. Скрипт бэкапа + тест восстановления (пауза)\n"
        "2. Загрузка в хранилище\n"
        "3. Ротация по расписанию\n"
        "4. Шифрование\n"
        "5. Cron + алерты"
    ),
    verification="Запустить бэкап → восстановить в тестовое окружение → данные совпадают",
    upsell=["Point-in-time recovery", "Cross-region репликация", "Автоматизированный DR-тест"],
    spec_hint="Зафиксируй: что бэкапить, хранилище, расписание, шифрование.",
))
