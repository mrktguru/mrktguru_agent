from .._types import WorkflowDef, register

register(WorkflowDef(
    id="gosuslugi_oauth",
    task_type="integration",
    label="ЕСИА / Госуслуги OAuth",
    keywords=["есиа", "госуслуги", "esia", "gosuslugi auth", "вход через госуслуги"],
    credits_min=1500,
    credits_max=2500,
    key_pause="после одобрения заявки в СМЭВ/Ростелеком — тест авторизации на preprod",
    questionnaire=(
        "1. Система: ЕСИА (Госуслуги) или региональный портал?\n"
        "2. Какие данные нужны из профиля (ФИО, СНИЛС, ИНН, документы)?\n"
        "3. Тип юридического лица (госорган, коммерция, НКО)?\n"
        "4. Нужна ли проверка статуса аккаунта (подтверждённый/упрощённый)?\n"
        "5. Стек: Python (authlib), Java, Node?"
    ),
    phases=(
        "1. Регистрация системы в ЕСИА + ключи (пауза)\n"
        "2. OAuth2 flow (code → token)\n"
        "3. Получение данных пользователя\n"
        "4. Маппинг на локальный аккаунт\n"
        "5. Тест на preprod → prod"
    ),
    verification="Войти через Госуслуги → данные профиля получены; уровень аккаунта проверен",
    upsell=["Подпись документов через Госключ", "СМЭВ-запросы", "Аудит авторизаций"],
    spec_hint="Зафиксируй: данные профиля, тип юрлица, стек.",
))
