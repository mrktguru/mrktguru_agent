from .._types import WorkflowDef, register

register(WorkflowDef(
    id="figma_to_site",
    task_type="figma_to_site",
    label="Figma → сайт",
    keywords=["figma", "фигма", "макет", "дизайн figma", "верстка по макету", "по дизайну"],
    credits_min=2000,
    credits_max=4000,
    key_pause="после анализа дизайна — покажи структуру секций перед вёрсткой",
    questionnaire=(
        "1. Ссылка на Figma-макет (убедитесь, что файл открыт по ссылке)?\n"
        "2. Стек вёрстки: чистый HTML/CSS, React, или Next.js?\n"
        "3. Нужна ли форма → куда отправлять (email, webhook)?\n"
        "4. Адаптив: только desktop или mobile тоже?"
    ),
    phases=(
        "1. Получить дизайн из Figma (структура + скриншот)\n"
        "2. Разбить на секции, показать пользователю (пауза)\n"
        "3. Сверстать HTML/CSS по каждой секции\n"
        "4. Адаптив, форма, meta-теги, деплой"
    ),
    verification="Открыть в браузере — сравнить с макетом, проверить адаптив 375/768/1280",
    upsell=["SEO-оптимизация", "Форма с интеграцией CRM", "Анимации при скролле"],
    spec_hint="Зафиксируй: список секций из дизайна, цвета, шрифты, наличие формы.",
))
