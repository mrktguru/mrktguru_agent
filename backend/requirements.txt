# Core
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.7.0
python-multipart==0.0.20

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10
alembic==1.14.0
pgvector==0.3.6

# Auth & Security
python-jose[cryptography]==3.3.0
bcrypt==4.2.1
cryptography==44.0.0

# AI
# >=0.69 required for adaptive thinking (thinking={"type":"adaptive"}) and the
# Claude 4.6/4.8 model strings used in the LLM layer registry.
anthropic>=0.69.0
# OpenAI embeddings for solution reuse vector search (optional — set OPENAI_API_KEY)
openai>=1.0.0

# Background tasks
celery[redis]==5.4.0
redis==5.2.1

# SSH
paramiko==3.5.0

# WebSockets handled by FastAPI/Starlette already

# Utilities
httpx==0.28.1
python-dotenv==1.0.1
email-validator==2.2.0
playwright==1.49.0
